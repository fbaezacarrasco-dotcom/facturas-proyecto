# Backend environment variables example

# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Toggle in-memory mode (skip DB). 1 to enable, 0 to disable
SKIP_DB_INIT=0

# Postgres connection (use DATABASE_URL or PG* fields)
# DATABASE_URL=postgres://user:password@localhost:5432/facturas
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=facturas

# Auth/JWT
JWT_SECRET=change_this_secret
# Token lifetime (e.g. 12h, 7d, 30d)
JWT_EXPIRES_IN=8h

# Optional: seed initial admin on first run if no users exist
ADMIN_EMAIL=admin
ADMIN_PASSWORD=admin123123

# CAPTCHA (opcional)
# Proveedor: turnstile | recaptcha | hcaptcha (solo turnstile implementado por defecto)
CAPTCHA_ENABLED=0
CAPTCHA_PROVIDER=turnstile
CAPTCHA_SECRET=your_turnstile_secret_key

# Para reCAPTCHA v2 (checkbox) — si usas este proveedor, define:
# CAPTCHA_PROVIDER=recaptcha
# CAPTCHA_SECRET=your_recaptcha_secret_key

# CAPTCHA simple (interno) — pregunta matemática básica
# CAPTCHA_PROVIDER=simple
# CAPTCHA_SIMPLE_TTL=120s
# CAPTCHA_SIMPLE_SECRET=optional_overrides_default (por defecto usa JWT_SECRET)
